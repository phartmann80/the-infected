# IP Ownership and Asset Provenance

Every asset must record source, owner, license/rights status, approval, version, and intended reuse surfaces. AI-generated assets must include provider, prompt reference, generation date, model/tool where available, and human approval state.
