# Branching and Contribution Rules

- Do not push directly to `main`.
- Use `feat/*`, `fix/*`, `docs/*`, `ci/*`, or `chore/*` branches.
- Use conventional commits.
- Pull requests require passing registry validation, secret scan, lint, typecheck, and build.
- No secrets, signing files, keystores, or private environment files in commits.
