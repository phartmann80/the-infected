ffmpeg -y -framerate 24 -i frames/frame_%05d.png -i mixed_audio.ogg \
  -c:v libtheora -q:v 6 -c:a libvorbis -q:a 4 \
  -pix_fmt yuv420p -s 1280x720 -r 24 cinematic_representative.ogv

# MP4 review copy:
ffmpeg -y -i cinematic_representative.ogv -c:v libx264 -preset fast -crf 23 -c:a aac -b:a 128k cinematic_review.mp4

# Frame generation: Python/Pillow (generate_frames.py)
# Audio: FFmpeg lavfi flite (temporary narration) + sine + aecho (temporary music)
# Server: AMD EPYC, 8 cores, no GPU
# Encode time: ~45 seconds total (5s frame gen + 3s audio mix + 37s video encode)