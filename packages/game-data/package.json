{
  "name": "@the-infected/game-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run src/index.test.ts",
    "validate:export": "node --import tsx ../../scripts/game-data/export.ts --check",
    "build": "tsc --noEmit && npm run test && npm run validate:export"
  }
}
